# 客户端 Web UI 优化计划

## 📋 优化目标

### 1. **字体系统优化**
- ✅ 统一字体族（使用现代无衬线字体）
- ✅ 统一字体大小层级（标题、正文、小字）
- ✅ 统一行高（lineHeight）
- ✅ 统一字间距（letterSpacing）
- ✅ 统一字体粗细（fontWeight）

### 2. **文本对齐优化**
- ✅ 统一文本对齐方式（左对齐、居中、右对齐）
- ✅ 优化多语言文本对齐（中文、英文、缅甸语）
- ✅ 统一表单标签和输入框对齐
- ✅ 统一按钮文本对齐

### 3. **UI 设计优化**
- ✅ 统一颜色系统（主色、次色、背景色、文字色）
- ✅ 统一间距系统（padding、margin、gap）
- ✅ 统一圆角系统（borderRadius）
- ✅ 统一阴影系统（boxShadow）
- ✅ 统一响应式断点

### 4. **组件样式优化**
- ✅ 导航栏：统一间距、对齐、响应式
- ✅ 表单：统一输入框、按钮、标签样式
- ✅ 卡片：统一圆角、阴影、内边距
- ✅ 按钮：统一大小、间距、对齐
- ✅ 模态框：统一布局、对齐、间距

## 🎨 设计系统规范

### 字体系统
```css
/* 字体族 */
font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'PingFang SC', 'Microsoft YaHei', sans-serif;

/* 字体大小层级 */
--font-size-xs: 0.75rem;    /* 12px */
--font-size-sm: 0.875rem;   /* 14px */
--font-size-base: 1rem;     /* 16px */
--font-size-lg: 1.125rem;   /* 18px */
--font-size-xl: 1.25rem;    /* 20px */
--font-size-2xl: 1.5rem;    /* 24px */
--font-size-3xl: 1.875rem;  /* 30px */
--font-size-4xl: 2.25rem;   /* 36px */

/* 行高 */
--line-height-tight: 1.25;
--line-height-normal: 1.5;
--line-height-relaxed: 1.75;

/* 字间距 */
--letter-spacing-tight: -0.025em;
--letter-spacing-normal: 0;
--letter-spacing-wide: 0.025em;
```

### 颜色系统
```css
/* 主色（蓝色渐变） */
--color-primary: #428cc9;
--color-primary-light: #468dda;
--color-primary-dark: #5587a4;

/* 文字颜色 */
--color-text-primary: #1a202c;
--color-text-secondary: #4a5568;
--color-text-tertiary: #718096;
--color-text-white: #ffffff;

/* 背景颜色 */
--color-bg-primary: #ffffff;
--color-bg-secondary: #f7fafc;
--color-bg-overlay: rgba(0, 0, 0, 0.5);
```

### 间距系统
```css
/* 间距单位（基于 4px 网格） */
--spacing-1: 0.25rem;  /* 4px */
--spacing-2: 0.5rem;   /* 8px */
--spacing-3: 0.75rem;  /* 12px */
--spacing-4: 1rem;     /* 16px */
--spacing-5: 1.25rem;  /* 20px */
--spacing-6: 1.5rem;   /* 24px */
--spacing-8: 2rem;     /* 32px */
--spacing-10: 2.5rem;  /* 40px */
--spacing-12: 3rem;    /* 48px */
```

### 圆角系统
```css
--radius-sm: 0.25rem;   /* 4px */
--radius-md: 0.5rem;   /* 8px */
--radius-lg: 0.75rem;  /* 12px */
--radius-xl: 1rem;     /* 16px */
--radius-full: 9999px;
```

### 阴影系统
```css
--shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
--shadow-md: 0 4px 6px rgba(0, 0, 0, 0.1);
--shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.1);
--shadow-xl: 0 20px 25px rgba(0, 0, 0, 0.15);
```

## 📱 响应式断点

```css
/* 移动端 */
@media (max-width: 767px) { }

/* 平板 */
@media (min-width: 768px) and (max-width: 1023px) { }

/* 桌面端 */
@media (min-width: 1024px) { }
```

## ✅ 优化清单

### 首页 (HomePage.tsx)
- [ ] 统一导航栏字体大小和对齐
- [ ] 优化表单输入框对齐和间距
- [ ] 统一按钮样式和对齐
- [ ] 优化卡片布局和间距
- [ ] 统一文本对齐方式

### 服务页 (ServicesPage.tsx)
- [ ] 统一标题和副标题对齐
- [ ] 优化卡片内容对齐
- [ ] 统一图标和文字对齐

### 跟踪页 (TrackingPage.tsx)
- [ ] 统一搜索框对齐
- [ ] 优化结果展示对齐
- [ ] 统一状态标签对齐

### 联系页 (ContactPage.tsx)
- [ ] 统一联系信息对齐
- [ ] 优化卡片内容对齐
- [ ] 统一图标和文字对齐

## 🚀 实施步骤

1. **创建全局样式文件** (`src/styles/global.css`)
2. **创建设计系统变量** (`src/styles/variables.css`)
3. **更新各页面组件**，应用统一样式
4. **测试响应式设计**
5. **优化多语言文本对齐**

