# Production 签名：必须使用与 Google Play「上传密钥」一致的 upload-release.keystore
# SHA1: AA:FA:1E:8C:F7:B1:ED:5C:97:DE:C2:87:AB:89:5A:5F:E7:88:13:97
MYAPP_RELEASE_STORE_FILE=upload-release.keystore
MYAPP_RELEASE_KEY_ALIAS=upload
MYAPP_RELEASE_STORE_PASSWORD=3o4VtmdIzjGXdIcVwOfQIFITnlOW2Bps
MYAPP_RELEASE_KEY_PASSWORD=3o4VtmdIzjGXdIcVwOfQIFITnlOW2Bps
android.useAndroidX=true
android.enableJetifier=false
hermesEnabled=true

# 增加 Java 堆内存
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# 启用并行构建
org.gradle.parallel=true

# 禁用配置缓存（解决构建错误）
org.gradle.configuration-cache=false

# 启用并行构建
org.gradle.parallel=true

# 启用构建缓存
org.gradle.caching=true

# 禁用 Jetifier
android.enableJetifier=false

# Specifies whether the app is configured to use edge-to-edge via the app config or plugin
# WARNING: This property has been deprecated and will be removed in Expo SDK 55. Use `edgeToEdgeEnabled` or `react.edgeToEdgeEnabled` to determine whether the project is using edge-to-edge.
expo.edgeToEdgeEnabled=true

# Use this property to enable edge-to-edge display support.
# This allows your app to draw behind system bars for an immersive UI.
# Note: Only works with ReactActivity and should not be used with custom Activity.
edgeToEdgeEnabled=true
