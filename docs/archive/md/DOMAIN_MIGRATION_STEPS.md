# 域名迁移步骤（market-link-express.com）

## 🎯 目标

将 `market-link-express.com` 从后台管理项目迁移到客户端 Web 项目。

## ⚠️ 重要提示

**是的，必须先移除，再添加！**

一个域名不能同时绑定到两个 Netlify 项目。所以需要：
1. 先从 `market-link-express` 项目移除
2. 再添加到 `client-ml-express` 项目

## 📋 详细步骤

### 步骤 1：从 market-link-express 项目移除域名

1. **在当前页面（market-link-express 的域名管理）：**
   - 找到 `market-link-express.com`（在域名列表底部）
   - 点击右侧的 **"Options"** 按钮（灰色按钮，带下拉箭头）
   - 选择 **"Remove"** 或 **"Unlink"**
   - 确认移除

2. **注意事项：**
   - 移除后，`market-link-express.com` 会暂时无法访问
   - 这是正常的，因为我们马上要把它添加到另一个项目

### 步骤 2：在 client-ml-express 项目中添加域名

1. **切换到 client-ml-express 项目：**
   - 在 Netlify Dashboard 顶部，点击项目名称下拉菜单
   - 选择 **"client-ml-express"**

2. **进入域名管理：**
   - 左侧导航：**"Domain management"**
   - 或直接访问：https://app.netlify.com/projects/client-ml-express/settings/domain

3. **添加域名：**
   - 点击 **"Add a domain"** 按钮
   - 输入：`market-link-express.com`
   - 点击 **"Verify"** 或 **"Add"**

4. **配置 DNS：**
   - Netlify 会显示需要配置的 DNS 记录
   - 通常是一个 CNAME 记录，指向 `client-ml-express.netlify.app`

### 步骤 3：配置 DNS（如果需要）

如果你的域名注册商还没有配置正确的 DNS：

```
类型    主机名    值
CNAME   @         client-ml-express.netlify.app
```

或者如果使用 www：
```
CNAME   www       client-ml-express.netlify.app
```

### 步骤 4：等待 DNS 生效

- 通常需要 **5-30 分钟**
- 最多可能需要 **24 小时**
- 可以在 Netlify Dashboard 中查看域名验证状态

## ✅ 最终域名配置

配置完成后：

| 项目 | Netlify 项目名 | 域名 | 用途 | 状态 |
|------|---------------|------|------|------|
| 客户端 Web | `client-ml-express` | `market-link-express.com` | 客户下单、跟踪 | ✅ 待配置 |
| 后台管理 | `market-link-express` | `admin-market-link-express.com` | 管理员后台 | ✅ 已配置 |

## ⚠️ 注意事项

1. **DNS 缓存：**
   - 移除域名后，DNS 缓存可能需要时间清除
   - 如果添加后无法立即访问，请等待几分钟

2. **SSL 证书：**
   - Netlify 会自动为域名配置 SSL 证书
   - 可能需要几分钟时间

3. **临时访问：**
   - 在域名迁移期间，可以使用 Netlify 默认域名：
   - 客户端 Web: https://client-ml-express.netlify.app
   - 后台管理: https://admin-market-link-express.netlify.app

## 🔍 验证步骤

迁移完成后：

1. **检查客户端 Web：**
   - 访问：https://market-link-express.com
   - 应该看到客户端 Web 的首页（下单页面）

2. **检查后台管理：**
   - 访问：https://admin-market-link-express.com
   - 应该看到后台管理登录页面

3. **检查 SSL：**
   - 两个域名都应该有 HTTPS（绿色锁图标）

## 🚨 如果遇到问题

1. **域名无法添加：**
   - 检查是否已从原项目完全移除
   - 等待几分钟后重试

2. **DNS 不生效：**
   - 检查域名注册商的 DNS 配置
   - 使用 `dig` 或 `nslookup` 命令检查 DNS 记录

3. **SSL 证书问题：**
   - 等待 Netlify 自动配置（通常几分钟）
   - 如果超过 24 小时，联系 Netlify 支持

